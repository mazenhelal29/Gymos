-- ============================================================
-- GymOS — ملاحظات حفظ بيانات الصالات
-- ============================================================
-- • تسجيل الخروج يحذف جلسة Auth فقط — لا يحذف members/payments/subscriptions.
-- • كل صف مربوط بـ gym_id ولا يُمسح إلا بحذف الصالة (ON DELETE CASCADE).
-- • لا تشغّل TRUNCATE أو DELETE عام على الجداول في الإنتاج.
-- • النسخ الاحتياطي: Supabase Dashboard → Database → Backups
